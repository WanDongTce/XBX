<compontNavbar title="分享" isShowBack="{{true}}"></compontNavbar>
<view class="box">
    <view class="tit">长按保存二维码，发送给好友进行游戏</view>

    <canvas wx:if="{{showCanvas}}" catch:longtap="saveImg" canvas-id="mycanvas" style="width:{{size.w}}px;height: {{size.h}}px;"/>

    <view class="btns">
        <button class="btn" open-type="share" plain="true">立即分享</button>
        <!-- <button class="btn" catchtap="collection" plain="true">收藏游戏</button>   -->
    </view>
</view>